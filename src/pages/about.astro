---
import Layout from '../layouts/Layout.astro';
---

<Layout title="About | MoneyJuan" description="About MoneyJuan and our mission to help Filipinos build wealth.">
	<div class="container page-content">
		<h1 class="page-title">About MoneyJuan</h1>
		
		<div class="content-wrapper">
			<section class="mission">
				<h2>Our Mission</h2>
				<p>
					MoneyJuan is a personal finance blog for people in the Philippines who want simple, practical, judgment-free money advice that actually fits real life.
				</p>
				<p>
					We believe that financial literacy shouldn't be reserved for the wealthy. Whether you're a salary earner, a freelancer, or an OFW, you deserve to understand how to manage your money and build a secure future.
				</p>
			</section>

			<section class="values">
				<h2>What We Stand For</h2>
				<div class="grid values-grid">
					<div class="value-item">
						<h3>Simple</h3>
						<p>No jargon without explanation. We speak plain English.</p>
					</div>
					<div class="value-item">
						<h3>Practical</h3>
						<p>Examples use Philippine prices, systems, and realities.</p>
					</div>
					<div class="value-item">
						<h3>Encouraging</h3>
						<p>No shaming. We focus on small wins and progress.</p>
					</div>
				</div>
			</section>

			<section class="disclaimer">
				<h3>Disclaimer</h3>
				<p>
					Content on MoneyJuan is for educational purposes only and does not constitute professional financial, legal, or tax advice. Please consult with a licensed professional for your specific situation.
				</p>
			</section>
		</div>
	</div>
</Layout>

<style>
	.page-content {
		padding: var(--space-lg) var(--space-sm);
		max-width: 800px;
	}

	.page-title {
		text-align: center;
		margin-bottom: var(--space-lg);
	}

	.content-wrapper {
		display: flex;
		flex-direction: column;
		gap: var(--space-lg);
	}

	.mission p {
		font-size: 1.1rem;
		line-height: 1.8;
		color: var(--color-text-muted);
	}

	.values-grid {
		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
		gap: var(--space-md);
		margin-top: var(--space-md);
	}

	.value-item {
		background: var(--color-surface);
		padding: var(--space-md);
		border-radius: 8px;
		border: 1px solid var(--color-border);
	}

	.value-item h3 {
		color: var(--color-primary);
		font-size: 1.2rem;
		margin-bottom: var(--space-xs);
	}

	.value-item p {
		font-size: 0.9rem;
		margin-bottom: 0;
	}

	.disclaimer {
		margin-top: var(--space-md);
		padding: var(--space-md);
		background: rgba(239, 71, 111, 0.1);
		border-left: 4px solid var(--color-accent);
		border-radius: 4px;
	}

	.disclaimer h3 {
		font-size: 1.1rem;
		color: var(--color-accent);
	}

	.disclaimer p {
		font-size: 0.9rem;
		margin-bottom: 0;
	}

	@media (max-width: 600px) {
		.page-content {
			padding: var(--space-md) var(--space-sm);
		}

		.page-title {
			margin-bottom: var(--space-md);
		}

		.mission p {
			font-size: 1rem;
		}

		.value-item,
		.disclaimer {
			padding: var(--space-sm);
		}
	}
</style>
